# LiveKit Agents (1.6 line) + plugins
# LLM = Groq (llama-3.3-70b-versatile); STT/TTS = Deepgram; VAD = Silero
livekit-agents[deepgram,groq,silero]~=1.6
livekit-api~=1.0

# Token server
fastapi
uvicorn[standard]
python-dotenv

# Postgres: per-user notes (writer_app schema) + auth user store (user_schema),
# bcrypt + JWT. Encryption at rest is handled by Postgres/Supabase.
psycopg[binary]
bcrypt
pyjwt
