# LiveKit Agents (1.6 line) + plugins
# LLM = Groq (llama-3.3-70b-versatile); STT/TTS = Deepgram; VAD = Silero
livekit-agents[deepgram,groq,silero]~=1.6
livekit-api~=1.0

# Token server
fastapi
uvicorn[standard]
python-dotenv

# Notes store — two backends:
#   * SQLCipher / AES-256 SQLite (local/offline dev + migration source)
#   * PostgreSQL (Supabase) via psycopg3; [binary] bundles libpq (no extra apt),
#     [pool] adds the thread-safe psycopg_pool.ConnectionPool.
sqlcipher3
psycopg[binary,pool]
